# POS Restro Hotel — Frontend

Next.js (App Router) + TypeScript + Tailwind frontend for the restaurant/hotel POS.

## Setup

```bash
npm install
cp .env.local.example .env.local
npm run dev
```

Expects the backend running at the URL in `.env.local` (default `http://localhost:8000/api`).
